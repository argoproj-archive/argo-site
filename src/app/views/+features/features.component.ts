import { Component } from '@angular/core';

import { PageSettings, HasPageSettings } from '../../common';

@Component({
    selector: 'argo-features',
    template: '<router-outlet></router-outlet>',
})
export class FeaturesComponent {
}
